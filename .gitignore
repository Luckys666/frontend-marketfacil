# Node / deps
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (build-*.js scripts são versionados; build/ é a saída deles)
build/

# Inject temporários (gerados durante deploy ao Bubble — clipboard payloads)
_temp_bubble_code.json
_inject_code.js
_chunk_*.json
clip-out.txt

# Agent tooling (não-produto)
.claude/
.codebase-memory/
ruvector.db

# Secrets (defensive — não devem existir no repo)
.env
.env.local
.env.*.local
*.key
*.pem
*credentials*
*tokens*
*oauth*

# IDE
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln

# OS
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
